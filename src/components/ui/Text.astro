---
interface Props {
  variant?: 'body' | 'lead' | 'small' | 'muted' | 'accent';
  as?: 'p' | 'span' | 'div';
  class?: string;
}

const {
  variant = 'body',
  as: Tag = 'p',
  class: className = '',
} = Astro.props;

const variants = {
  body: 'text-body text-white leading-relaxed',
  lead: 'text-h4 text-white leading-relaxed font-light',
  small: 'text-small text-white',
  muted: 'text-body text-white',
  accent: 'text-body text-white font-medium',
};

const classes = `${variants[variant]} ${className}`;
---

<Tag class={classes}>
  <slot />
</Tag>
